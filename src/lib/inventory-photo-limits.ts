/** Max edge length for inventory product thumbnails before upload. */
export const INVENTORY_PHOTO_MAX_EDGE_PX = 400

export const INVENTORY_PHOTO_JPEG_QUALITY = 0.78

/** Target max file size (~200KB); compression loop may stop slightly above. */
export const INVENTORY_PHOTO_MAX_BYTES = 204800
