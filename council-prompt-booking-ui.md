# Moved

The full-app council prompt (operator + portal + booking + auth + settings + sheets + everything) is now:

**[council-prompt-ui.md](./council-prompt-ui.md)**

Copy everything below the `---` in that file into your external LLM council.

This file previously contained booking-only context; it has been superseded.
